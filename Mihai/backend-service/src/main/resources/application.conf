# Backend Service no longer connects directly to PostgreSQL.
# All database access is delegated to Data Service (http://data-service:8082).
# DATA_SERVICE_URL can be overridden via environment variable.
